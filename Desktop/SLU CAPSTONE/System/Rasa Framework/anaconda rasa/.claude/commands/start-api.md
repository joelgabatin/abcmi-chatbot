# Start Grace API Server

Start the custom Flask API server (Windows-friendly, port 8000).

```bash
conda run -n rasa310 python grace_api.py
```

Server will be available at:
- http://localhost:8000
- Webhook: http://localhost:8000/webhooks/rest/webhook
