display_name: PR Release Manager
short_description: Start from updated main, create a feature branch, then create a PR, merge it, and refresh local main.
default_prompt: Use the local PR automation agent to start from updated main, create a feature branch before work begins, and after verification create the PR, merge it into main, and pull the updated main locally.
